// Presenter overlay routes — rendered only inside their own content-protected
// Electron windows (see artifacts/desktop). Each is a standalone widget with no
// AppShell nav.
export { ControlsOverlay } from "./components/controls-overlay";
export { CameraOverlay } from "./components/camera-overlay";
export { NotesOverlay } from "./components/notes-overlay";
